(()=>{document.documentElement.classList.remove('no-js');const reduced=matchMedia('(prefers-reduced-motion: reduce)').matches;if(!reduced){document.addEventListener('pointermove',e=>{const card=e.target.closest('.b-card');if(!card)return;const r=card.getBoundingClientRect();card.style.setProperty('--mx',`${e.clientX-r.left}px`);card.style.setProperty('--my',`${e.clientY-r.top}px`)},{passive:true});const io=new IntersectionObserver(entries=>{for(const entry of entries){if(entry.isIntersecting)entry.target.dataset.visible='true'}},{rootMargin:'120px'});document.querySelectorAll('[data-reveal]').forEach(el=>io.observe(el))}document.addEventListener('shopify:section:load',e=>{e.target.querySelectorAll('[data-reveal]').forEach(el=>el.dataset.visible='true')})})();
